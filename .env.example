# Database Configuration (NeonDB)
# Copy this file to .env and update with your actual values
DATABASE_URL=postgresql://username:password@ep-example-123456.us-east-1.aws.neon.tech/dbname?sslmode=require

# Server Configuration
PORT=5432
NODE_ENV=development

# Instructions:
# 1. Go to https://neon.tech and create a new project
# 2. Create a database named 'world' or use the default database
# 3. Copy your connection string from the Neon dashboard
# 4. Replace the DATABASE_URL value above with your actual connection string
# 5. Make sure to include ?sslmode=require at the end of the connection string
# 6. Run the database_setup.sql script in your NeonDB SQL editor
